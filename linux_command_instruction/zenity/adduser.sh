#!/bin/bash

    # zenity --info --text "Hi!"
    # sleep 0.5
    # zenity --question --text="Would you like ot participate in a little form?"
    # sleep 1
    zenity --width=622 --height=400 --info --text="Getting important run files"
